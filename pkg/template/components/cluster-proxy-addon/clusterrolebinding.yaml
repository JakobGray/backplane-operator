apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ .Org }}:cluster-proxy-addon:addon-manager
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ .Org }}:cluster-proxy-addon:addon-manager
subjects:
  - kind: ServiceAccount
    name: cluster-proxy
    namespace: '{{ .Namespace }}'
